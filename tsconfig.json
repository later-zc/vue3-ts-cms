{
  "extends": "@vue/tsconfig/tsconfig.web.json",
  "include": [
    "env.d.ts",
    "src/**/*",
    "src/**/*.vue",
    "auto-imports.d.ts",
    "components.d.ts"
  ],
  "exclude": ["commitlint.coinfig.js"],
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "@/*": ["./src/*"]
    }
    // "noImplicitAny": false
    // "types": ["element-plus/global"] // 按需引入无需配置，全局引入配置
  },

  "references": [
    {
      "path": "./tsconfig.config.json"
    }
  ]
}
